# KAI Terminal — Frontend

This document has been merged into the root [`README.md`](../README.md).

See the root README for:
- Tech stack and features
- Getting started (prerequisites, secrets, running)
- Frontend development commands (`npm run dev`, `npm run build`, `npm run lint`)
- Architecture overview
