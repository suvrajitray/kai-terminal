# KAITerminal.RiskEngine

This document has been merged into the root [`README.md`](../../README.md).

See the **Profit Protection** section for:
- Risk evaluation order (Hard SL → Target → Trailing SL)
- Trailing SL example with floor mechanics
- Risk engine configuration (`RiskEngine:*` keys)
- State lifecycle (in-memory, resets on restart)
- Risk engine log messages
- Risk event notifications pipeline

See the **Architecture** section for the dependency graph and `IUserTokenSource` integration.
