# KAITerminal.Api

This document has been merged into the root [`README.md`](../../README.md).

See the **API Reference** section for all endpoints:
- Auth (Google OAuth, JWT)
- Upstox — Auth, Positions, Orders, Options
- Zerodha — auth flow, positions, orders
- Master Data (unified option contracts)
- Risk Configuration
- AI Signals
- Internal (Worker → Api risk event relay)
- WebSocket Hubs (PositionsHub, IndexHub, RiskHub)
- Diagnostics

See the **Getting Started** section for configuration and secrets setup.
