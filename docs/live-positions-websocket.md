# Live Positions via WebSocket

This document has been merged into the root [`README.md`](../README.md).

See the **Live Positions WebSocket** section for:
- Hub architecture and connection URL
- Server → Client message protocol (`ReceivePositions`, `ReceiveLtpBatch`, `ReceiveOrderUpdate`)
- Exchange filter (`?exchange=NFO,BFO`)
- Frontend live P&L calculation
- Hosting notes (Nginx, Azure App Service, Docker)
